// fruit.interface.ts

/**
 * Interface représentant un fruit.
 */
export interface Fruit {
    id: number; // Identifiant unique du fruit.
    name: string; // Nom du fruit.
    price: number; // Prix du fruit.
    quantity: number; // Quantité disponible du fruit.
  }
  
